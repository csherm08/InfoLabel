function setup() {
	createCanvas( 600, 400);
  p1 = new Arc()
}

function draw() {
  background(0)
  stroke("white")
  ellipse(width/2, height/2, 10, 10)
  noFill()
  p1.show()
  for(int i = 0; i < 10; i++){
    p1.update(i/20)
  }
}
